#API key required for upload and status endpoints. replace with yours and make sure the same with forntend UPLOAD_API_KEY
UPLOAD_API_KEY=supersecret123
# HMAC secret used to sign download URLs. Set in production and rotate securely.
DOWNLOAD_SECRET=replace_with_a_long_random_hex_or_base64_string
# Signed download expiry in seconds (default used by server if unset)
DOWNLOAD_EXPIRES_SEC=3600